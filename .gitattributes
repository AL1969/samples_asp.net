# Defining line endings for several file types - there is no need to set it at the git client side
# see: https://help.github.com/articles/dealing-with-line-endings

# Set the default behavior, in case people don't have core.autocrlf set.
* text=auto

# enforce Windows line endings, because this was the setting in Perforce before
* text eol=crlf

*.cs text
*.cpp text
*.inl text
*.c text
*.h text
*.hpp text
*.list text
*.spec text
*.sh text
*.mak text

# Test lists should be always Unix like
#*.txt text eol=lf

# Declare files that will always have CRLF line endings on checkout.
*.sln text eol=crlf
*.bat text eol=crlf

# Denote all files that are truly binary and should not be modified.
wkhtmltopdf binary
*.dll binary
*.lib binary
*.xlsx binary
*.xls binary
*.docx binary
*.doc binary
*.pdb binary
*.exe binary
*.obj binary
*.so binary
*.so.* binary
*.yaa binary
*.png binary
*.bmp binary
*.cur binary
*.jpg binary
*.a binary
*.o binary
*.ico binary
*.ICO binary
*.ttf binary
